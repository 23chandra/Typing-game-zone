---
import Layout from '../../../layouts/Layout.astro';
import FAQSection from '../../../components/FAQSection.astro';
import { getLocalizedFAQData, generateFAQSchema } from '../../../lib/faqData';
import { SUPPORTED_LOCALES, type SupportedLocale } from '../../../i18n/config';
import { t } from '../../../i18n/utils';

export function getStaticPaths() {
  return SUPPORTED_LOCALES.map(lang => ({
    params: { lang },
    props: { lang }
  }));
}

const { lang } = Astro.params as { lang: SupportedLocale };
const currentLocale = lang || 'en';

const localizedFAQ = getLocalizedFAQData(currentLocale);
const seoTitle = `${t('footer.faq', currentLocale)} — ${t('seo.faqTitle', currentLocale)}`;
const seoDescription = t('seo.faqDescription', currentLocale);
const faqStructuredData = generateFAQSchema(localizedFAQ);
---

<Layout
  title={seoTitle}
  description={seoDescription}
  locale={currentLocale}
  structuredData={faqStructuredData}
>
  <div class="py-12 sm:py-16 bg-canvas-soft min-h-screen">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-3xl mx-auto mb-10">
        <h1 class="text-3xl sm:text-5xl font-bold tracking-tight text-ink">
          {t('footer.faq', currentLocale)}
        </h1>
        <p class="mt-3 text-sm text-body leading-relaxed">
          {t('seo.faqDescription', currentLocale)}
        </p>
      </div>

      <FAQSection locale={currentLocale} includeSchema={false} />
    </div>
  </div>
</Layout>
